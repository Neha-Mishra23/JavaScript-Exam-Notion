// Object Method: Add a method getCarInfo to the car object that returns a string with the car's details.

let carObj = {
    model : "tesla",
    year : 1992,
}
console.log(`model of car is ${carObj.model} and ${carObj.year}`);