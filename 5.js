// Arrow Function: Write an arrow function multiply(a, b) that returns the product of two numbers.

const fun  = (a,b) =>
    {
        return a*b;
    }
    let multiply = fun(1,3);
    console.log(multiply);