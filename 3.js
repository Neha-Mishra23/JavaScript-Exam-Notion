// Function Declaration: Write a function greet(name) that takes a name as an argument and returns a greeting message.

function greet(name) {
    return `Hello, ${name}!`;
  }

  let message = greet("neha");
  console.log(message);  