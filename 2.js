// Comparison Operators: Write a function isEqual(a, b) that checks if two numbers are equal and returns true or false.

let compare=(a,b) =>{
    if(a>b){
        return true ;
    }
    else{
        return false
    }
}

let cum=compare(10,5);
console.log(cum);
